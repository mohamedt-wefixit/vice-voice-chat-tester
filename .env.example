# URL of the deployed backend (no trailing slash, no /api suffix)
# The app connects to the Socket.IO WebSocket at this URL.
VITE_BACKEND_URL=https://vice-git-main-mohamedt-wefixits-projects.vercel.app
